# settings template for app.py (web) or odm360.py (CLI)
[main]
# name of project (string)
name=default 
# number of cameras (int)
n_cams=
# time interval between photos (s)
dt=
# path to store photos for this project
root=
# verbose level logging (default False, meaning INFO level, True is DEBUG level)
verbose=
quiet=
